# Projection evolution rule

Add structure to the projection when a concrete research question needs it, not merely because the source record can be normalized further.

For example, stages and conditions already drive cross-corpus questions, so they have dedicated tables. Individual observations do not yet drive a demonstrated relational query, so they remain an ordered JSON array. This keeps the first D1 model small while preserving the ability to evolve it through migrations.
