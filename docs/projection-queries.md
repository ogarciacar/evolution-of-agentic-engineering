# Projection query intent

The first projection is not a generic content database. Its purpose is to interrogate the Evolution of Agentic Engineering evidence corpus.

The primary query axes are therefore the model implication, evolutionary stages, Selection conditions, producer, and time. These allow the corpus to reveal patterns without changing the epistemic separation between source-grounded observation and researcher interpretation that exists in canonical YAML.
